export enum MaterialColorVariants {
  primary = 'primary',
  accent = 'accent',
  warn = 'warn',
}

export enum MaterialButtonsDesignTypes {
  basic = 'mat-button',
  raised = 'mat-raised-button',
  stroked = 'mat-stroked-button',
  flat = 'mat-flat-button',
  icon = 'mat-icon-button',
  fab = 'mat-fab',
  miniFab = 'mat-mini-fab',
}
